---
import Layout from '../index.astro'
import { Code } from 'astro/components';
---

<Layout>
	<section id="why" class="no-padding">
		<h2 class="h3">Installation</h2>
		<div class="card">
			<div class="card-body">

				<a href="https://app.netlify.com/start/deploy?repository=https://github.com/netlify/netlify-statuskit"><img src="https://www.netlify.com/img/deploy/button.svg" alt="Deploy to Netlify"></a>
			
				<ul class="installation-steps">
					<li>If not already intalled, install latest <a href="https://nodejs.org/en/">Node version (Current)</a></li>
					<li>In the project directory run <code>npm install</code></li>
					<li>To start the project for development run <code>npm run dev</code></li>
					<li>To build the project for production <code>npm run build</code></li>
				</ul>

				<p></p> 
				<p></p>
				<p><strong>Please remember...</strong></p>
			</div>
		</div>
	</section>
	<section id="faq" class="no-padding">
		<div>
			<h2 class="h3">Contact Form</h2>
			<div class="card">
				<div class="card-body">
					<p>Open <code>php/contact.php</code> and specify recipient's e-mail address by filling <mark class="doc">$fromEmail</mark> and <mark class="doc">$sendToEmail</mark> options. These email addresses will receive all form inquires/messages.</p>
					<ul class="list-unstyled mb-0">
					<li><mark class="doc fw-normal bg-pale-primary">$fromEmail</mark> An email address that will be in the From field of the email</li>
					<li><mark class="doc fw-normal bg-pale-primary">$sendToEmail</mark> An email address that will receive the email with the output of the form.</li>
					</ul>
				</div>
				<!--/.card-body -->
				<div class="card-footer position-relative">
					<a class="btn btn-primary btn__first" data-toggle="collapse" data-target=".collapse" data-text="Collapse">View example's code</a>
				</div>
				<!--/.card-footer -->
			</div>
			<div class="collapse">
				<Code code={`// an email address that will be in the From field of the email.
$fromEmail = 'your-email@domain.com';
$fromName = 'Sandbox Contact Form';

// an email address that will receive the email with the output of the form
$sendToEmail = 'your-email@domain.com';
$sendToName = 'Sandbox Contact Form';`} lang="js" theme="monokai" />
			</div>
		</div>
	</section>
</Layout>

